---
title: 'Kafka → Iceberg Streaming Pipeline'
description: 'Real-time event streaming from Kafka topics into Apache Iceberg tables using Flink, with automatic compaction and schema evolution.'
status: 'active'
tags: ['kafka', 'apache-iceberg', 'flink', 'kubernetes']
github: ''
---

Building a production streaming pipeline that writes Kafka events directly into Iceberg tables. The goal is to replace a legacy batch-based ingestion system with sub-minute latency.
